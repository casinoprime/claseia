import { login, signup } from './actions'
export default async function Login({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams;return <main className="login"><section className="card"><div className="brand">Clase<span>IA</span></div><h1>Estudiá desde tus clases reales</h1><p className="muted">Subí o grabá una clase. ClaseIA la transcribe y te ayuda a preparar el parcial.</p>{q.error&&<p className="error">{q.error}</p>}{q.check&&<p>Revisá tu email para confirmar la cuenta.</p>}<form className="form"><input className="input" name="email" type="email" placeholder="Email" required/><input className="input" name="password" type="password" placeholder="Contraseña" minLength={6} required/><div className="row"><button className="primary" formAction={login}>Ingresar</button><button className="ghost" formAction={signup}>Crear cuenta</button></div></form></section></main>}
